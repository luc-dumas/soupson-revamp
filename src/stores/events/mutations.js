
export const eventsMutations = {
  SET_EVENTS(state, events) {
    state.events = events;
  },
  //! Implement later
  // SET_EVENT_NAME(state, event_name) {
  //   state.event_name = event_name;
  // },
  // SET_EVENT_DETAILS(state, event_details) {
  //   state.event_details = event_details;
  // },
  // SET_EVENT_DATE(state, event_date) {
  //   state.event_date = event_date;
  // },
  // SET_EVENT_IMAGE(state, event_image) {
  //   state.event_image = event_image;
  // },
  // SET_EVENT_SOUPCON(state, event_soupcon) {
  //   state.event_soupcon = event_soupcon;
  // },
  // SET_EVENT_HOST(state, event_host) {
  //   state.event_host = event_host;
  // },
  // SET_EVENT_AUTHOR(state, event_author) {
  //   state.event_author = event_author;
  // },
  // SET_EVENT_VENUE(state, event_venue) {
  //   state.event_venue = event_venue;
  // },
  // SET_VENUE_ADDRESS(state, venue_address) {
  //   state.venue_address = venue_address;
  // },
};
