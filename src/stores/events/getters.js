export default {
  // user(state, getters, rootState, rootGetters) {
  //   return rootGetters['auth/isAuthenticatedAsLearner'] ? state.learner : state.instructor;
  // },
  // fullName(state, getters) {
  //   return `${getters?.user?.firstName} ${getters?.user?.lastName}`.trim();
  // },
  // initials(state, getters) {
  //   return `${getters?.user?.firstName?.charAt(0)}${getters?.user?.lastName?.charAt(0)}`.trim();
  // },
};